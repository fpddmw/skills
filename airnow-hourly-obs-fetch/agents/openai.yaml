interface:
  display_name: "AirNow Hourly Observations"
  short_description: "Retrieve bounded AirNow site-hour observations"
  default_prompt: "Use $airnow-hourly-obs-fetch to retrieve preliminary AirNow site-hour observations for a bounded UTC window and region."
