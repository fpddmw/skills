interface:
  display_name: "Eco Council Normalize"
  short_description: "Normalize eco-council run data"
  default_prompt: "Use $eco-council-normalize to convert eco-council raw fetch outputs into canonical claims, observations, evidence cards, and round context JSON."
