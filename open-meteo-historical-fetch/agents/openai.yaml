interface:
  display_name: "Open-Meteo Historical Weather"
  short_description: "Retrieve bounded historical weather reanalysis"
  default_prompt: "Use $open-meteo-historical-fetch to retrieve a bounded historical weather reanalysis window for known coordinates."
